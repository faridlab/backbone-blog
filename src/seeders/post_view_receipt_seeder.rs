//! PostViewReceipt seeder
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Seeds initial PostViewReceipt data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// POSTVIEWRECEIPT SEEDER
// ============================================================================

/// Seeder for post_view_receipt
pub struct SeedPostViewReceiptSeeder;

impl SeedPostViewReceiptSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedPostViewReceiptSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedPostViewReceiptSeeder {
    fn name(&self) -> &'static str {
        "SeedPostViewReceiptSeeder"
    }

    fn order(&self) -> i32 {
        5
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if blog.post_view_receipts table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM blog.post_view_receipts")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/post_view_receipt_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM blog.post_view_receipts")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>
